export enum CompletionStatus {
  completed = 'completed',
  pending = 'pending',
  missed = 'missed',
  cancelled = 'cancelled',
  deleted = 'deleted',
}

export enum CollectorStatus {
  decline = 'decline',
  accept = 'accept',
}
